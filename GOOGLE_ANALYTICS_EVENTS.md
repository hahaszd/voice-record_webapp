# 📊 Google Analytics 事件跟踪配置

## ✅ 已添加的事件跟踪

### 1. **录音相关事件**

#### `recording_started` - 开始录音
```javascript
gtag('event', 'recording_started', {
    'event_category': 'Recording',
    'event_label': 'User started recording',
    'audio_source': 'microphone' | 'system' | 'both'
});
```
**触发时机**：用户点击录音按钮开始录音

**追踪数据**：
- 录音次数
- 使用的音频源（麦克风/系统/两者）

#### `recording_cancelled` - 取消录音
```javascript
gtag('event', 'recording_cancelled', {
    'event_category': 'Recording',
    'event_label': 'User cancelled recording'
});
```
**触发时机**：用户点击取消按钮

**追踪数据**：
- 取消录音的次数
- 用户行为模式

---

### 2. **转录相关事件**

#### `transcription_started` - 开始转录
```javascript
gtag('event', 'transcription_started', {
    'event_category': 'Transcription',
    'event_label': 'User started transcription',
    'requested_duration': 30 | 60 | 300
});
```
**触发时机**：用户点击转录按钮

**追踪数据**：
- 转录次数
- 用户选择的转录时长（30s/1m/5m）

#### `transcription_completed` - 转录成功
```javascript
gtag('event', 'transcription_completed', {
    'event_category': 'Transcription',
    'event_label': 'Transcription successful',
    'text_length': 245,
    'duration': 30
});
```
**触发时机**：转录成功完成

**追踪数据**：
- 成功转录的次数
- 转录文本长度
- 音频时长

#### `transcription_failed` - 转录失败
```javascript
gtag('event', 'transcription_failed', {
    'event_category': 'Transcription',
    'event_label': 'Transcription failed',
    'error_message': 'Error description'
});
```
**触发时机**：转录失败

**追踪数据**：
- 失败次数
- 失败原因

---

### 3. **用户交互事件**

#### `copy_button_clicked` - 手动复制
```javascript
gtag('event', 'copy_button_clicked', {
    'event_category': 'Interaction',
    'event_label': 'User copied text manually',
    'text_length': 245
});
```
**触发时机**：用户点击复制按钮

**追踪数据**：
- 手动复制次数
- 复制文本长度

#### `auto_copy_success` - 自动复制成功（页面可见）
```javascript
gtag('event', 'auto_copy_success', {
    'event_category': 'AutoCopy',
    'event_label': 'Auto-copy successful (page visible)',
    'text_length': 245
});
```
**触发时机**：自动复制功能开启，转录完成时页面可见

**追踪数据**：
- 自动复制使用次数

#### `auto_copy_on_visible` - 页面激活自动复制
```javascript
gtag('event', 'auto_copy_on_visible', {
    'event_category': 'AutoCopy',
    'event_label': 'Auto-copied when page became visible',
    'text_length': 245
});
```
**触发时机**：页面从隐藏变为可见，自动复制待复制文本

**追踪数据**：
- 页面激活复制次数
- 多任务使用场景

#### `audio_source_changed` - 音频源切换
```javascript
gtag('event', 'audio_source_changed', {
    'event_category': 'Settings',
    'event_label': 'Changed to microphone',
    'audio_source': 'microphone' | 'system' | 'both'
});
```
**触发时机**：用户切换音频源

**追踪数据**：
- 用户偏好的音频源
- 功能使用分布

---

## 📊 在 Google Analytics 中查看数据

### 1. 登录 Google Analytics 4

访问：https://analytics.google.com/

### 2. 查看实时数据

1. 左侧菜单 → **Reports** → **Realtime**
2. 可以看到：
   - 当前活跃用户
   - 实时事件
   - 用户来源

### 3. 查看事件报告

1. 左侧菜单 → **Reports** → **Engagement** → **Events**
2. 可以看到所有自定义事件：
   - `recording_started`
   - `transcription_started`
   - `transcription_completed`
   - `copy_button_clicked`
   - 等等

### 4. 创建自定义报告

**推荐报告 1：用户录音行为**
- 指标：`recording_started` 事件数
- 维度：`audio_source`（音频源）
- 图表：饼图

**推荐报告 2：转录成功率**
```
成功率 = transcription_completed / transcription_started × 100%
```

**推荐报告 3：功能使用分布**
- `recording_started` - 录音使用次数
- `transcription_started` - 转录使用次数
- `copy_button_clicked` - 手动复制次数
- `auto_copy_success` - 自动复制次数

---

## 🎯 关键指标（KPI）

### 用户参与度
```
每日活跃用户 (DAU)
每用户平均录音次数 = recording_started / 用户数
每用户平均转录次数 = transcription_started / 用户数
```

### 功能健康度
```
转录成功率 = transcription_completed / transcription_started
录音完成率 = transcription_started / recording_started
（如果用户录音后没转录，说明可能遇到问题或放弃）
```

### 功能使用偏好
```
音频源分布：
- 麦克风使用率
- 系统音频使用率
- 两者结合使用率

复制方式偏好：
- 手动复制 vs 自动复制
- 页面激活复制使用率
```

---

## 🔍 高级分析

### 1. 用户流程分析

典型用户流程：
```
页面访问
  ↓
audio_source_changed (选择音频源)
  ↓
recording_started (开始录音)
  ↓
transcription_started (开始转录)
  ↓
transcription_completed (转录成功)
  ↓
copy_button_clicked 或 auto_copy_success (复制文本)
```

### 2. 流失点分析

**流失点 1**：`recording_started` → `transcription_started`
- 如果差距大，说明用户录音后没转录
- 可能原因：权限问题、录音失败、用户放弃

**流失点 2**：`transcription_started` → `transcription_completed`
- 如果转录失败率高
- 需要检查：API 错误、音频质量、服务稳定性

### 3. 用户留存

跟踪：
- 首次访问用户行为
- 回访用户行为
- 功能重复使用率

---

## 📈 GA4 事件参数详解

### Event Category（事件类别）
- `Recording` - 录音相关
- `Transcription` - 转录相关
- `Interaction` - 用户交互
- `AutoCopy` - 自动复制
- `Settings` - 设置变更

### Event Label（事件标签）
- 具体的操作描述
- 易于理解的文本说明

### Custom Parameters（自定义参数）
- `audio_source` - 音频源类型
- `text_length` - 文本长度
- `duration` - 时长
- `error_message` - 错误信息
- `requested_duration` - 请求的转录时长

---

## 🎨 创建自定义仪表板

### 推荐的仪表板卡片

#### 卡片 1：核心指标
```
今日录音次数: XX
今日转录次数: XX
转录成功率: XX%
活跃用户数: XX
```

#### 卡片 2：功能使用
```
音频源分布:
- 麦克风: XX%
- 系统音频: XX%
- 两者: XX%
```

#### 卡片 3：用户行为
```
复制方式:
- 手动复制: XX次
- 自动复制: XX次
- 页面激活复制: XX次
```

#### 卡片 4：趋势图
```
过去7天：
- 录音次数趋势线
- 转录次数趋势线
- 用户增长曲线
```

---

## 🔧 调试和验证

### 本地测试

1. 打开网站
2. 打开浏览器 Console (F12)
3. 执行操作（录音、转录、复制）
4. 在 Console 中检查：
   ```javascript
   // 手动触发测试事件
   gtag('event', 'test_event', {
       'event_category': 'Test',
       'event_label': 'Testing GA integration'
   });
   ```
5. 在 GA4 实时报告中查看（延迟约 10-30 秒）

### Network 调试

1. 打开 DevTools → Network 标签
2. 过滤：`collect` 或 `google-analytics`
3. 执行操作
4. 应该看到发送到 GA 的请求

---

## 📝 隐私和合规

### GDPR 合规

当前实现：
- ✅ 不收集个人身份信息
- ✅ 只收集匿名使用数据
- ✅ 用户可以通过浏览器设置禁用

建议（如面向欧盟用户）：
- 添加 Cookie 同意横幅
- 提供隐私政策链接
- 允许用户选择退出追踪

### 数据保留

Google Analytics 4 默认：
- 事件数据保留 2 个月
- 用户数据保留 14 个月

可以在 GA4 设置中调整。

---

## 🎯 后续优化建议

### 可以添加的其他事件

1. **转录时长选择**
   ```javascript
   gtag('event', 'duration_selected', {
       'event_category': 'Settings',
       'duration': 30 | 60 | 300
   });
   ```

2. **自动录音切换**
   ```javascript
   gtag('event', 'auto_record_toggled', {
       'event_category': 'Settings',
       'enabled': true | false
   });
   ```

3. **历史记录操作**
   ```javascript
   gtag('event', 'history_opened', {
       'event_category': 'Interaction'
   });
   ```

4. **错误追踪**
   ```javascript
   gtag('event', 'error_occurred', {
       'event_category': 'Error',
       'error_type': 'microphone_permission' | 'api_error'
   });
   ```

---

## 📊 预期的数据洞察

### 用户行为洞察

1. **最常用的功能**
   - 哪个音频源最受欢迎？
   - 用户更喜欢手动复制还是自动复制？

2. **使用模式**
   - 用户一次会话录音多少次？
   - 平均录音时长是多少？

3. **问题点**
   - 哪里的失败率最高？
   - 用户在哪里流失？

### 产品优化方向

基于数据决策：
- 如果自动录音使用率低 → 考虑改进 UI 或说明
- 如果某个音频源使用率极低 → 考虑简化选项
- 如果转录失败率高 → 优先修复转录稳定性

---

## 🚀 部署说明

所有 GA 事件已添加到代码中：
- ✅ 录音开始/取消
- ✅ 转录开始/成功/失败
- ✅ 复制操作（手动/自动/页面激活）
- ✅ 音频源切换

**无需额外配置**，Google Analytics 会自动开始收集这些事件。

---

## 📖 查看报告的步骤

### 实时查看（测试用）

1. 访问 https://analytics.google.com/
2. 选择你的网站属性
3. Reports → Realtime
4. 在网站上执行操作
5. 10-30 秒后在实时报告中看到事件

### 历史报告（分析用）

1. Reports → Engagement → Events
2. 选择事件名称（如 `recording_started`）
3. 查看：
   - 事件计数
   - 事件参数
   - 用户分布
   - 时间趋势

---

## 🎓 最佳实践

### 事件命名规范

我们使用的规范：
- 小写字母
- 下划线分隔
- 动词_名词 格式（如 `recording_started`）

### 参数命名

- `event_category` - 功能模块
- `event_label` - 具体描述
- 自定义参数 - 相关业务数据

### 不要过度追踪

✅ 追踪关键行为和决策点
❌ 不追踪每个微小交互（会淹没有用数据）

---

## 🔒 数据安全

### 我们不收集的数据
- ❌ 转录的文本内容
- ❌ 音频文件
- ❌ 个人身份信息
- ❌ IP 地址（GA4 自动匿名化）

### 我们收集的数据
- ✅ 事件计数（点击次数）
- ✅ 功能使用频率
- ✅ 文本长度（数字）
- ✅ 设置选项（音频源类型）
- ✅ 匿名用户 ID（GA4 自动生成）

---

## 📞 问题排查

### 事件未出现在 GA4

1. **检查 gtag 加载**
   ```javascript
   // 在 Console 中运行
   console.log(typeof gtag);  // 应该是 'function'
   ```

2. **检查 Measurement ID**
   ```html
   <!-- index.html 中 -->
   <script async src="https://www.googletagmanager.com/gtag/js?id=G-75D37JVX99"></script>
   ```

3. **检查网络请求**
   - DevTools → Network
   - 应该看到发送到 `google-analytics.com/g/collect`

4. **等待数据延迟**
   - 实时报告：10-30 秒延迟
   - 标准报告：24-48 小时延迟

---

**设置完成日期**: 2026-02-04  
**GA Property ID**: G-75D37JVX99  
**事件数量**: 8 个核心事件  
**状态**: ✅ 已完成并推送到 dev 分支
