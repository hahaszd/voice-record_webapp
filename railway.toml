[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m uvicorn server2:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
