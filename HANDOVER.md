# HANDOVER — 2026-07-28

> 这是**接力棒**，不是维护文档。读完、跟着指针走完之后**删掉或重写它**。
> 过期的 handover 比没有更糟——它看起来很权威。

## 1. 现状

- **全部已提交并推送，`dev` == `main`，生产已部署验证。**
- 测试：后端 pytest **31/31**、Playwright **49/49**（functional + smoke-chrome + recording）。
  `mobile` project 有已知的历史失败，不阻塞（见 `tests/EVAL_CHECKLIST.md`）。
- 本次唯一的**运行时**改动是 v123（cache-bust 自动化），已在 `voicespark.app` 实测：
  三个页面都返回内容哈希、资源可加载、哈希与本地文件 sha256 一致。其余全是文档与测试。
- 无半途而废的改动，无未跑的测试。

## 2. 下一步

`BACKLOG.md` 里三条，都不紧急，按性价比排：

1. **`'you'` 静音幻觉进文本层**（`BACKLOG.md` 🔵）。现在段落层能拦住，但拦法是抛异常触发
   fallback，白打一次下游 API。⚠️ `'you'` 是极常见英文词，**必须锚定整段**（`^you$` 之类）
   才能加进 `_HALLUCINATION_PHRASES`，否则会误删真实语音——参见 v114 的教训。
   `tests/backend/test_hallucination_filter.py` 已有误伤防护用例，可直接扩。
2. **OpenAI Project 预算上限**（`BACKLOG.md` 🔵）。纯控制台操作，**owner 才能做**。
   比 scope 限制更强的防线——受限 key 挡不住 chat/images（父级 scope 一开就全通）。
3. **N2**（`tests/EVAL_CHECKLIST.md`）：全幻觉时最终要给用户看到"未识别到文字"。
   注意 N1 只验到"过滤器抛异常触发降级"这一层，**N2 是端到端行为，没被覆盖**。

## 3. 注意事项（这次踩到的坑的"形状"）

- **别用测不出目标维度的实验去推翻既有结论。** 这次拿干净录音室样本做转录模型 A/B，三个模型
  全部转对——那份测试**根本区分不出中文准确率**，而那正是 owner 2026-02 否决候选模型的维度。
  差点据此推翻一个正确的旧决策。
- **变异测试不是可选项。** N1 最初那条"位置感知"不变式看着很稳，实际**抓不到阈值被放松**
  （非开头是合取、开头是析取，结构差异让断言恒成立）。不做变异测试就会交付一套假绿的测试。
- **不要改生产代码去迁就测试。** 空 segments 抛异常看着可疑，查清楚是生产不可达且行为更安全，
  于是改测试记录真实行为，而不是改代码让返回值好看。
- **靠人脑维护的编号必然漂移。** 版本号混乱的根因不是"编号乱"，是两个语义不同的计数器长得
  一样、且都没有唯一权威来源。能自动算出来的就别让人记（v123 已把 cache-bust 自动化）。

## 4. 等 owner

- **无阻塞项。** 上一个待确认的事（改 handover 写 `HANDOVER.md`）已按 owner 指示完成。
- 备忘：`.env` 里的 `OPENAI_API_KEY` 现在是**可用状态**，`test_model_ab.py` 会拿它打真实 API。
  文件已被 `.gitignore` 忽略、从未进过版本库。

---

**指针**：`BACKLOG.md`（没做的）· `DECISION_LOG.md`（决策/否决 + **✅已验证结论速查**，
动手查证前先读它）· `tests/EVAL_CHECKLIST.md`（测试缺口）· `VERSION_HISTORY.md`（代码变更）·
`CLAUDE.md`（三条铁律 + 架构事实）。
